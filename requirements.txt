# ==========================================================
# CẤU HÌNH DEPENDENCIES - PHẦN MỀM CHẤM CÔNG & QUÉT MẶT
# Khuyến nghị: Python 3.12 (64-bit)
# ==========================================================

# --- Core Web Server & Networking ---
flask>=3.0.0
Werkzeug>=3.0.0
requests>=2.31.0

# --- Xử lý ảnh & Computer Vision ---
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=9.5.0
piexif>=1.1.3

# --- Nhận diện khuôn mặt (DeepFace & TensorFlow) ---
deepface>=0.0.90
tensorflow>=2.15.0
tf-keras>=2.15.0

# --- Nhận diện chữ & Watermark Timestamp (EasyOCR & PyTorch) ---
torch>=2.0.0
torchvision>=0.15.0
easyocr>=1.7.0

# --- Xử lý file Excel & Word ---
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
python-docx>=1.1.0

# --- Xử lý file PDF (Trích xuất & Chuyển đổi Word) ---
PyMuPDF>=1.23.0
pdf2docx>=0.5.8

# --- Tẩy vết watermark AI (Tùy chọn) ---
remove-ai-watermarks>=0.41.0

# --- OCR truyền thống qua Tesseract (Tùy chọn) ---
pytesseract>=0.3.10

# --- Đóng gói EXE & Testing ---
pyinstaller>=6.0.0
pytest>=8.0.0
